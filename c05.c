#include<stdio.h>
int main(int argc, char const *argv[])
{
    float a,b;
    printf("enter two no. for difference");
    scanf("%f%f",&a,&b);
    printf("difference of two no. is %f",a-b);
    return 0;
}