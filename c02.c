#include<stdio.h>
int main(int argc, char const *argv[])
{
    float a,b;
    printf("enter 2 no.");
    scanf("%f%f",&a,&b);
    printf("first no. is %f second no. is %f\n",a,b);
    printf("product of two no is %f",a*b);
    return 0;
}
